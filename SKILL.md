---
name: zepp-health
description: >
  This skill should be used when the user asks about their health data,
  wants a health analysis, health report, sleep analysis, recovery status,
  training readiness, body battery, stress level, or says "健康", "身体",
  "睡眠", "恢复", "训练", "运动", "心率", "HRV", "压力", "血氧".
---

# Zepp Health Analysis Skill

通过 Zepp/Amazfit API 获取用户的健康数据，进行个性化分析和建议。

## 数据获取

运行以下命令获取结构化健康数据快照：

```bash
python3 {skill_dir}/scripts/health_snapshot.py
```

可指定日期：

```bash
python3 {skill_dir}/scripts/health_snapshot.py --date YYYY-MM-DD
```

输出 JSON 包含：当日数据、7 天趋势、运动记录、评分明细。

## 配置

需要配置 Zepp API 认证信息，按优先级：
1. `{skill_dir}/config.json` — `{ "app_token": "...", "user_id": "...", "host": "api-mifit-cn3.zepp.com" }`
2. 环境变量 `ZEPP_COOKIE` — cookie 字符串
3. 环境变量 `ZEPP_APP_TOKEN` + `ZEPP_USER_ID`

获取 cookie：登录 app.zepp.com，从浏览器开发者工具复制 Cookie。

## 分析框架

拿到数据后，参考 `{skill_dir}/references/health_analysis_guide.md` 进行分析。

核心分析维度：

### 1. 恢复状态
- HRV 与个人基线对比（趋势比绝对值重要）
- RHR 与个人基线对比
- 连续 3 天 HRV↓ + RHR↑ = 疲劳信号

### 2. 睡眠质量
- 深睡占比 15-20%、REM 占比 20-25% 为正常
- 醒来次数 <3 次为佳
- 睡眠效率 >85% 为佳

### 3. 训练负荷（心率储备法）
- inactive: 无运动数据
- detraining: <75 min/week，训练不足
- low: 75-150 min/week，偏低
- productive: 150-300 min/week，WHO 推荐范围
- overreaching: 300-450 min/week，负荷偏高
- high_risk: >450 min/week，过度训练风险

### 4. 身体电量
- >70 适合训练，<30 需要休息

### 5. 压力与血氧
- 压力均值 <40 为低压力，>60 需关注
- 血氧 <93% 需建议就医

## 输出格式

按以下结构组织分析结果（用中文输出）。

**分隔线规则（很重要，避免满屏横线）：**
- ═══════ 只用于 5 个大段之间的分隔（共出现 4 次）
- ─── 只用于仪表盘内部的子区块标题
- 热力图和对比卡**不加边框**，用标题行 + 空行自然分隔
- 同一区块内不要连续出现两条分隔线

### 第一行：状态摘要（必须）

用一句话 + emoji 给出整体判断，让用户扫一眼就知道今天身体怎么样：

    今天的健康状态：🟢 良好 — 恢复充分，适合中高强度训练
    今天的健康状态：🟡 一般 — 昨晚睡眠不足，建议降低训练强度
    今天的健康状态：🔴 需注意 — HRV连续3天下降，建议休息

状态灯选择标准：
- 🟢 良好：主要指标正常或优于基线，无明显风险
- 🟡 一般：个别指标偏低但不严重，需要适当调整
- 🔴 需注意：多项指标异常或有明确风险信号，需要重视

═══════════════════════════════════════════════════

### 指标概览 + 评分（仪表盘）

用方案A格式展示，指标和评分合并在一个区块内：

    ─── 指标概览 ─────────────────────
    HRV     52ms   ↑3%   🟢
    RHR     56bpm  →     🟢
    睡眠    7.2h   ↓8%   🟡
    深睡    18%          🟢
    电量    72           🟢
    压力    45           🟡
    血氧    97%          🟢
    ─── 评分 ─────────────────────────
    综合   66/100        🟡
    恢复   54/100        🟡
    睡眠   79/100        🟢
    运动   无数据        ⚪
    ──────────────────────────────────

每个指标一行：指标名 + 数值 + 趋势箭头（如有）+ 状态灯。
趋势箭头规则：↑ 明显上升 | ↓ 明显下降 | → 基本持平 | 波动大用 ↑↓
状态灯规则：🟢 正常 | 🟡 略有偏差 | 🔴 异常 | 🟠 数据缺失 | ⚪ 无数据
评分状态灯：<50 🔴 | 50-69 🟡 | 70-84 🟢 | 85+ 💪

═══════════════════════════════════════════════════

### 7天趋势

⚠️ **不要用 ASCII sparkline 画折线图**（┤┌─●─╲╱ 那种）。
中英文混排终端里字符宽度不一致，● 和日期轴永远对不齐，用户多次反馈"对不齐"。
不要画带连接线的折线图，不要尝试用 ╲╱ 连接不同高度的点。

**默认方案：对比卡 + 热力图（方式A）**

先用对比卡展示多指标今天 vs 基线 vs 昨天，再用热力图展示波动大的指标的7天走势。

第一层 — 今日对比卡：

    今日对比
             今天    基线    昨天    趋势
    HRV      52ms    47ms    46ms    ✓ 回升
    RHR      55bpm   55bpm   52bpm   → 持平
    电量     81      75      83      ↓ 小降
    睡眠     77分    75分    73分    ↑ 连续回升

对比卡规则：
- 列出 4-6 个核心指标（HRV、RHR、电量、睡眠必选，压力/准备度/深睡按需加）
- 今天列：当日数值；基线列：个人基线值；昨天列：前一天数值
- 趋势列：✓ 回升 / ↓ 下降 / → 持平 / ✗ 恶化
- 只选有变化的指标，全部平稳的可以不列

第二层 — 热力图（仅对波动大的指标展开）：

    HRV 7天 (基线: 47ms)
    基线: 47ms
     47  52  47  47  51  46  52
      ──  🟢  ──  ──  🟢  🔴  🟢
      +0  +5  +0  +0  +4  -1  +5

热力图规则：
- 只对 7 天内波动 >5% 的指标画热力图，平稳的跳过
- 三行信息：绝对值 → 基线对比灯(🟢🔴──) → 偏离幅度(+N/-N)
- 基线对比灯：高于基线 🟢 | 低于基线 🔴 | ±2%以内 ──
- 每个指标一个区块，用标题行 + 空行自然分隔，不加边框

备选方案 — 文字趋势（兜底）：

    HRV: 47→52→47→47→51→46→52（波动中，今天回升到高点）

只对波动 >5% 的指标展示，平稳的跳过或一句话说明。

═══════════════════════════════════════════════════

### 交叉分析

多指标关联判断，保持文字叙述。段内不需要特殊格式，
但每个分析点之间用空行分隔，避免大段文字堆在一起。

示例：
    HRV 高于基线 + 电量 81 + 压力仅 31，恢复指标全面向好。

    综合分只有 66 是因为运动分为 0 拉低了总分，
    实际上身体恢复状态很好，纯粹是缺少运动刺激。

    昨晚醒来 5 次（理想 <3 次），虽然总时长够，
    但频繁醒来打断深睡连续性。

═══════════════════════════════════════════════════

### 个性化建议（带分级符号）

每条建议前加符号标明优先级：

    ⚠️  重要：HRV连续3天下降，今晚务必在22:00前入睡
    💡  建议：深睡占比偏低，可以试试睡前1小时不看手机
    ✅  正常：压力水平在合理范围，继续保持

符号说明：
- ⚠️ 重要：需要立即调整或高度关注的事项
- 💡 建议：可以优化的方面，非紧急
- ✅ 正常：状态好的指标，给予正面反馈

最多列 5 条建议，按优先级从高到低排列。

═══════════════════════════════════════════════════

### 今日行动（checklist 格式）

用清单格式，让用户当 to-do list 用：

    📋 今日行动清单
    □ 22:00前放下手机，准备入睡
    □ 白天可以做30分钟有氧运动
    □ 减少咖啡摄入（下午2点后不喝）
    □ 记录今天的情绪状态

规则：
- 最多 4 条行动项
- 每条必须具体可执行（时间、地点、动作），不要泛泛而谈
- 用 □ 表示待完成

### 末尾：数据说明（可选）

仅在数据不完整时出现，正常情况下不加：

    ⚠️ 数据说明：压力数据缺失（设备未佩戴），以上分析基于可用指标

## 多日分析

当用户说"前两天"时，指的是**两天都要分析**（如 5/17 和 5/18），不是只取其中一天。
对每个日期分别调用 `--date` 获取数据，然后做并排对比。
详见 `references/multi-day-comparison.md`。

用户语言匹配：用户用中文问就用中文答，用英文问就用英文答。

## 安全边界

以下情况必须建议就医，不做诊断：
- SpO2 < 90%
- 静息心率异常升高超过基线 20% 以上
- HRV 持续异常偏低（低于基线 30%+）
- 用户描述胸痛、呼吸困难等症状

始终保持在健康建议范围内，不提供医疗诊断。

## 测试与调试

修完代码后用以下流程验证：

```bash
# 1. 依赖检查
python3 {skill_dir}/scripts/health_snapshot.py --check

# 2. 从 /root 运行（验证配置路径）
python3 {skill_dir}/scripts/health_snapshot.py --date 2026-05-19

# 3. 检查 _meta 字段确认 API 状态
# 4. 用无效 token 测试错误提示是否清晰
# 5. 测试 config fallback（临时重命名 skill 的 config.json）
```

详细排查流程见 `references/debugging-null-data.md`。

## 向 CC 提交修复任务

当发现 skill 问题需要 CC 修复时，写任务文档到 `~/.hermes/plans/zepp-health-*.md`，
包含：问题描述、原因分析、修复方案、涉及文件、测试方法。
然后告诉 CC：「读一下 ~/.hermes/plans/zepp-health-xxx.md，按方案修」。

## Demo 模板

`{skill_dir}/templates/demo-report.html` 是一个完整的示例报告 HTML，
用假数据展示了所有输出格式（状态摘要、仪表盘、对比卡、热力图、建议、行动清单）。
深色主题，适合截图作为 GitHub README 的示意图。

修改方法：打开 HTML 文件，替换数值和文案，浏览器截图即可。

## 免责声明

本工具仅供个人健康数据参考，不构成医疗建议。使用逆向工程 API 接口，与 Zepp Health 无关联。用户需自行确保符合平台服务条款。
